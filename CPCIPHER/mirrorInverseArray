#include<bits/stdc++.h>
using namespace std;

int main(){
  int flag = true;
  vector<int>ar={3, 4, 2, 9, 1};
  for(int i=0; i<ar.size(); i++){
    if(i!=ar[ar[i]]){
      flag=false;
    }
  }
  if(flag==true)
    cout<<"mirror inverse";
  else
  cout<<"not a mirror inverse";
}
